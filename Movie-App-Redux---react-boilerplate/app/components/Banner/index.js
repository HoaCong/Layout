/**
 *
 * Banner
 *
 */

import { Box } from '@material-ui/core';
import React, { memo } from 'react';
// import PropTypes from 'prop-types';
// import styled from 'styled-components';

function Banner() {
  return <Box color="red">BAnner</Box>;
}

Banner.propTypes = {};

export default memo(Banner);
